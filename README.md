# PDF Merger

A local, in-browser PDF and image merger with drag-and-drop ordering and smart sorting.

**Features**
- Merge PDFs and common image formats into a single PDF
- Drag-and-drop file import
- Manual reordering plus multiple sort modes (name, date, size, type)
- Runs locally in your browser using `pdf-lib` (no uploads)
- Custom output filename

